jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.6-tem
  - sdk use java 17.0.6-tem
install:
  - ./gradlew :reimagined:assembleRelease :reimagined-hilt:assembleRelease :reimagined-material3:assembleRelease -x :sample:build -x :samples-di:anvil:build -x :samples-di:dagger:build -x :samples-di:hilt-assistedInject:build -x :samples-di:hilt-hiltViewModel:build -x :samples-di:koin:build
